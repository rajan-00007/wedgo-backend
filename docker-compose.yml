services:
  api:
    image: ${HARBOR_URL:-registry.local}/${HARBOR_PROJECT_BACKEND:-wedgo}/wedgobackend:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: always